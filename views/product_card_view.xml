<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="website_sale_products_item_inherit" inherit_id="website_sale.products_item">
        <xpath expr="//div[contains(@class, 'o_wsale_product_sub')]" position="inside">
            <div class="w-100 mt-3" style="font-size: 12px;">
                <strong>
                    <t t-esc="product.empaquetado_descripcion" />
                </strong>
            </div>
            <div class="w-100 mt-2 text-muted font-weight-bold"> $<t t-esc="product.precio_unitario" />
                / un </div>
        </xpath>
    </template>
</odoo>